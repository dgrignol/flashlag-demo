import React from "react";
import ReactDOM from "react-dom/client";
import FlashLagGame from "./FlashLagGame";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FlashLagGame />
  </React.StrictMode>
);